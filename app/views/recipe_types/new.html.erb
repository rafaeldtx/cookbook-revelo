<% if @recipe_type.errors.any? %>
    <% @recipe_type.errors.each do |attribute, message| %>
        <%= message %>
    <% end %>
<% end %>

<%= form_for @recipe_type do |f| %>
<%= f.label :name, "Nome" %>
<%= f.text_field :name %>
<%= f.submit "Enviar" %>
<% end %>